name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest

      - name: Run unit tests (excluding API/network-dependent tests)
        run: |
          python -m pytest tests/ -v --ignore=tests/test_api.py --ignore=tests/test_stress.py --ignore=tests/test_dashboard.py --ignore=tests/test_evaluate.py
